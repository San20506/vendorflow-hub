---
phase: 02-backend-integration
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified: [
  "src/pages/Dashboard.tsx",
  "src/pages/Orders.tsx",
  "src/pages/Insights.tsx",
  "src/lib/queries.ts",
  "src/hooks/useProducts.ts",
  "src/hooks/useOrders.ts",
  "src/hooks/useSettlements.ts"
]
autonomous: true
---

<objective>
## Goal
Replace mock data with real Supabase queries across all pages — transform UI from read-only prototype to functional data-driven application.

## Purpose
Phase 2 Plan 01 established the schema and auth system. This plan wires up the UI to query real data from Supabase, establishing the patterns for all future data-driven features (inventory sync, analytics, reconciliation).

## Output
- Query hooks for all major data types (products, orders, settlements)
- Integration of queries into Dashboard, Orders, Insights pages
- Real-time data binding with TanStack React Query
- Error handling and loading states
- Foundation for future features (filters, pagination, real-time updates)
</objective>

<context>
## Project Context
@.paul/PROJECT.md
@.paul/ROADMAP.md
@.paul/STATE.md

## Prior Phase Work
@.paul/phases/02-backend-integration/02-01-SUMMARY.md
- Supabase schema with users, vendors, products, orders, settlements tables
- TypeScript types auto-generated from schema
- Authentication context with role-based access
- Demo data seeding via admin SDK

## Source Files
@src/pages/Dashboard.tsx (currently uses mock data)
@src/pages/Orders.tsx (currently uses mock data)
@src/pages/Insights.tsx (currently uses mock data)
@src/types/database.ts (generated schema types)
@src/integrations/supabase/client.ts (Supabase client with auth helpers)
@src/contexts/AuthContext.tsx (user context with role)
</context>

<acceptance_criteria>

## AC-1: Query Hooks Created
```gherkin
Given Supabase schema established (Plan 01)
When executing useProducts(), useOrders(), useSettlements() hooks
Then return typed data from Supabase tables with loading/error states
And handle role-based filtering (vendor sees own data only)
```

## AC-2: Dashboard Wired to Real Data
```gherkin
Given Dashboard page loads
When authenticated as vendor user
Then displays real products, orders, and settlement data from Supabase
And metrics (KPIs) calculated from real data
And no mock data references remain
```

## AC-3: Orders Page Functional
```gherkin
Given Orders page loads
When authenticated as vendor user
Then displays all orders for that vendor from Supabase
And can view order details
And returns show real data
And no mock data references remain
```

## AC-4: Insights Page Shows Real Metrics
```gherkin
Given Insights page loads
When authenticated as vendor user
Then displays real analytics calculated from orders, products, settlements
And charts show real data (not mock)
And filters work (by portal, status, date range)
And no mock data references remain
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Create query hooks for products, orders, and settlements</name>
  <files>src/lib/queries.ts, src/hooks/useProducts.ts, src/hooks/useOrders.ts, src/hooks/useSettlements.ts</files>
  <action>
    1. Create src/lib/queries.ts with database query functions:
       - getProducts(vendorId: string) → Products[] (from Supabase)
       - getOrders(vendorId: string) → Orders[] (from Supabase)
       - getSettlements(vendorId: string) → Settlements[] (from Supabase)
       - getReturns(vendorId: string) → Returns[] (from Supabase)
       - getExpenses(vendorId: string) → Expenses[] (from Supabase)
    
    2. Create custom React hooks in src/hooks/:
       - useProducts(vendorId): returns { data, isLoading, error, refetch }
       - useOrders(vendorId): returns { data, isLoading, error, refetch }
       - useSettlements(vendorId): returns { data, isLoading, error, refetch }
       - useReturns(vendorId): returns { data, isLoading, error, refetch }
       - useExpenses(vendorId): returns { data, isLoading, error, refetch }
    
    3. Use TanStack React Query (useQuery) for caching and refetching
    4. Implement role-based filtering: vendor sees only own data
    5. Handle loading and error states
    
    Avoid: Direct supabase calls in components (encapsulate in hooks); hardcoding vendorId
  </action>
  <verify>
    1. Build: npm run build — no errors
    2. Import: useProducts, useOrders from @/hooks — types resolve
    3. Verify hooks return proper shape: { data, isLoading, error, refetch }
    4. Manual: Call hook in component, verify data loads from Supabase
  </verify>
  <done>AC-1 satisfied: Query hooks created and returning real Supabase data</done>
</task>

<task type="auto">
  <name>Integrate Dashboard with real data</name>
  <files>src/pages/Dashboard.tsx</files>
  <action>
    1. Replace mock data imports with query hooks:
       - Remove mockData imports and inline mock data
       - Import useProducts, useOrders, useSettlements hooks
    
    2. Wire hooks to get authenticated user's vendor ID:
       - Use useAuth() to get current user role
       - For vendors, fetch their data; for admin, show aggregated view
    
    3. Calculate KPIs from real data:
       - Total Revenue = sum of order total_amounts
       - Active Products = count of products with stock > 0
       - Pending Orders = count of orders with status = 'pending'
       - Return Rate = count of returns / count of orders
    
    4. Update charts to use real data:
       - Order trend chart from orders.created_at
       - Revenue breakdown from orders by status
       - Product performance from order quantities
    
    5. Add loading states for each widget
    6. Handle errors gracefully
    
    Avoid: Multiple API calls per render (use hooks); hardcoded test data
  </action>
  <verify>
    1. Build: npm run build — no errors
    2. Run: npm run dev
    3. Login as vendor user (from demo data seed)
    4. Dashboard loads without errors
    5. KPI cards show real numbers (not mock values)
    6. Grep: grep -r "mockData\|mock" src/pages/Dashboard.tsx — should be empty
  </verify>
  <done>AC-2 satisfied: Dashboard displays real Supabase data, no mock references</done>
</task>

<task type="auto">
  <name>Integrate Orders page with real data</name>
  <files>src/pages/Orders.tsx</files>
  <action>
    1. Replace mock orders with real query:
       - Remove inline mock order data
       - Import useOrders hook
    
    2. Wire orders to authenticated user:
       - Filter to current user's vendor's orders (via vendor_id FK)
       - For admin users, show all orders
    
    3. Update order table columns to match schema:
       - order_id, product_id, quantity, total_amount, status, created_at
       - Add links to product details, settlement info
    
    4. Implement status filters:
       - Filter by status enum (pending, confirmed, shipped, delivered, cancelled)
       - Use TanStack table filtering or manual filter state
    
    5. Add returns integration:
       - Show return count per order
       - Link to return details if exists
    
    6. Add loading skeleton while data loads
    
    Avoid: Mocking orders table; hardcoding product names
  </action>
  <verify>
    1. Build: npm run build — no errors
    2. Run: npm run dev
    3. Navigate to Orders page
    4. Table populates with real orders (from demo seed)
    5. Filters work (select status, table updates)
    6. Grep: grep -r "mockData" src/pages/Orders.tsx — should be empty
  </verify>
  <done>AC-3 satisfied: Orders page displays real data, filters functional</done>
</task>

<task type="auto">
  <name>Integrate Insights page with real analytics</name>
  <files>src/pages/Insights.tsx</files>
  <action>
    1. Replace mock analytics with real calculations:
       - Remove hardcoded mock data arrays
       - Import query hooks (useOrders, useProducts, useSettlements)
    
    2. Calculate real metrics:
       - Total Revenue = sum(orders.total_amount)
       - Avg Order Value = Total Revenue / order count
       - Product Count = count(products)
       - Top SKU = product with max order quantity
       - Settlement Status = count by status (pending/processed)
    
    3. Wire date range filters:
       - Filter orders by created_at date range
       - Pass dates to query hooks (or filter in component)
    
    4. Wire status filters:
       - Filter orders by status enum
       - Filter settlements by status
    
    5. Update charts:
       - Revenue trend (LineChart from orders grouped by date)
       - Order status breakdown (PieChart from order statuses)
       - Settlement flow (BarChart from settlement amounts by status)
       - Product performance (top products by order count)
    
    6. Add loading spinners while queries run
    
    Avoid: Hardcoded mock values; calculations on fake data
  </action>
  <verify>
    1. Build: npm run build — no errors
    2. Run: npm run dev
    3. Navigate to Insights page
    4. Metrics display real calculated values
    5. Charts render with real data (from demo seed)
    6. Filters change chart data
    7. Grep: grep -r "mockData" src/pages/Insights.tsx — should be empty
  </verify>
  <done>AC-4 satisfied: Insights shows real analytics from Supabase data</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- src/types/database.ts (schema types, locked)
- src/integrations/supabase/client.ts (Supabase client, stable)
- src/contexts/AuthContext.tsx (auth system, stable)
- supabase/migrations/001_init_schema.sql (schema, locked)

## SCOPE LIMITS
- This plan wires existing UI pages to real data — does NOT add new pages or features
- Does NOT implement RLS policies (note: no RLS yet, all authenticated users can see all data — address in next plan)
- Does NOT add pagination (show all results for now; add pagination in future plan)
- Does NOT implement real-time subscriptions (handle in future plan)
- Does NOT add complex filtering UI (basic status/date filters only)

</boundaries>

<verification>
Before declaring plan complete:
- [ ] `npm run build` passes with no errors
- [ ] All mock data references removed from pages (grep confirms empty)
- [ ] Dashboard, Orders, Insights load without errors
- [ ] Authenticated user sees their own data only (vendor sees vendor data, admin sees all)
- [ ] KPIs and metrics calculated from real data
- [ ] No console errors in browser DevTools
- [ ] Demo data seed populates sufficient records for testing
</verification>

<success_criteria>
- All 4 tasks completed successfully
- No mock data remaining in UI pages
- Real Supabase queries working for products, orders, settlements
- Dashboard displays real KPIs
- Orders page shows real orders with filtering
- Insights page shows real analytics
- Build passing with no errors
- Ready for Phase 3 (Channel Connectors)
</success_criteria>

<output>
After completion, create `.paul/phases/02-backend-integration/02-02-SUMMARY.md` with:
- Query hook patterns established
- Integration approach (TanStack Query, custom hooks)
- Data binding working end-to-end
- Decisions: RLS deferred, pagination deferred, real-time deferred
- Next phase readiness: Foundation for channel syncs
</output>
